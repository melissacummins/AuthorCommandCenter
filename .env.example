# Supabase Configuration
# Get these from your Supabase project: Settings > API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Link Shortener
# Public base URL the UI shows when copying short URLs.
# Default uses the current site origin + /l. Set this to your custom subdomain
# (e.g. https://go.yourdomain.com) once DNS is configured in Vercel.
# VITE_SHORT_LINK_BASE_URL=https://go.yourdomain.com

# Server-side env vars (set these on Vercel for the redirect function — NOT prefixed with VITE_):
#   SUPABASE_URL                = same as VITE_SUPABASE_URL
#   SUPABASE_SERVICE_ROLE_KEY   = Supabase service role key (Settings > API)
#   IP_HASH_SALT                = any random string; rotates IP hashes if changed
